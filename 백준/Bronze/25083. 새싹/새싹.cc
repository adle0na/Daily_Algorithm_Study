#include <iostream>
#include <sstream>
#include <string>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // 글자 ' 자체를 넣고 싶다	\'
    // 글자 " 자체	\"
    // 글자 \ 자체	\\

    string s;

    cout << R"(         ,r'"7
r`-_   ,'  ,/
 \. ". L_r'
   `~\/
      |
      |
)";

    return 0;
}